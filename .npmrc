# Disable Prisma binary engine download during build
# This is to prevent issues with Vercel's build process
# The binary will be downloaded during the prisma generate step
node-linker=hoisted
public-hoist-pattern[]=*prisma*
